server:
  port: 8087

spring:
  application:
    name: KYC_SERVICE

  datasource:
    url: jdbc:mysql://mysql:3306/digital_wallet
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

sarvam:
  api-key: sk_josnolp3_53lQ7iTSi5cFqjUglXpnABna
  base-url: https://api.sarvam.ai/speech-to-text

springdoc:
  api-docs:
    path: /v3/api-docs

services:
  auth:
    url: http://auth-service:8081