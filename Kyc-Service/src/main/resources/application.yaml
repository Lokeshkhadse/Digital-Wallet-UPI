server:
  port: 8087

spring:
  application:
    name: KYC_SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/digital_wallet
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

# Vosk Configuration
#vosk:
#  model-path: "C:/vosk-model-small-en-us-0.15"

sarvam:
  api-key: sk_josnolp3_53lQ7iTSi5cFqjUglXpnABna
  base-url: https://api.sarvam.ai/speech-to-text



#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka

logging:
  level:
    org.hibernate.SQL: DEBUG